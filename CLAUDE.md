# 패밀리세일 모아보기 — 작업 가이드

인터넷·SNS에 흩어진 패밀리세일 정보를 모아 보여주는 단일 파일 정적 페이지.
저장소 구성은 `index.html` 하나가 전부이며, 데이터도 그 안의 `const SALES = [...]` 배열 하나다.
수정할 곳은 사실상 SALES 배열과 헤더의 "마지막 업데이트" 날짜뿐이다.

## "제보 업데이트 해줘" 워크플로우

제보는 GitHub 이슈가 아니라 **구글폼**으로 들어온다.

- 제보 폼: https://forms.gle/mnzZxKFG71woqF2T6 ("패밀리세일 제보") — 페이지 하단 제보 버튼과 연결됨
- 응답 시트: Google Sheets `1DR_p2b2-uWMujyugmcXxUpQKmnNrmKKYjq_EfdJmAVA` ("패밀리세일 제보(응답)")
  - 열: 타임스탬프 / 게시물 링크 또는 계정명 / 메모
  - 시트는 비공개이므로 소유자 계정(stepersjmj@gmail.com) 인증이 있는 환경에서만 읽을 수 있다
- 제보 링크의 주 출처: 딜링크(dealink.co.kr/familysale/N), Threads·Instagram @familysale

절차:

1. 응답 시트를 읽는다. Google Drive 연동 도구(`read_file_content`)로 fileId를 직접 읽는 것이
   브라우저 스크린샷보다 안정적이다 (URL이 화면에서 잘리는 문제 방지).
2. 각 링크를 WebFetch로 열어 브랜드·기간·할인율·판매 페이지를 확인한다.
3. 기존 카드와 중복 체크. 중복이면 추가하지 말고, 기간·할인율이 다르면 공식몰 기준으로 기존 카드를 보정한다.
4. SALES 배열에 카드 추가 (아래 작성 규칙 참고).
5. 헤더 "마지막 업데이트" 날짜 갱신. 날짜는 딜링크 게시일 등 외부 근거로 검증할 것
   (브라우저 미리보기 시계가 실제 날짜와 달랐던 사례 있음).
6. 로컬 렌더 확인(콘솔 에러 없음, 카드 수 정상) 후 커밋 + 푸시. 푸시까지가 관행이다.
   다른 PC에서 올린 커밋이 있을 수 있으니 푸시가 거부되면 `git pull --rebase`로 합류한다.
7. 반영 완료된 제보는 응답 시트에서 행 삭제 (승인된 관행). 삭제 전후로 시트를 다시 읽어
   의도치 않은 변경(오타 유입·서식 변경)이 없는지 검증한다. 폼 자체의 응답 카운트가 남는 것은 정상.

"웹에서 찾아서 업데이트"를 요청받으면 딜링크 목록(dealink.co.kr/familysale)과 @familysale 계정을 훑어
페이지에 없는 신규 세일을 같은 방식으로 추가한다. 검색 결과의 연도를 반드시 확인할 것 (작년 게시물이 섞여 나옴).

## SALES 카드 작성 규칙

- **브랜드별 1카드**: 한 게시물에 여러 브랜드가 각자 링크를 갖고 있으면(네이버 브랜드데이 등)
  묶지 말고 브랜드마다 카드를 1건씩 만든다.
- 필드: `brand`, `title`, `category`, `start`/`end`(YYYY-MM-DD, 미정 null), `confirmed`,
  `place`, `region`("온라인"/"서울"), `discount`, `note`, `link`(판매 페이지 — 있으면 카드 전체 클릭 가능),
  `source: {label, url}`
- `start`가 null이고 `end`만 있어도 진행 중으로 표시된다 ("~까지 (시작일 미상)").
- 종료된 세일은 지우지 않는다 — 날짜가 지나면 자동으로 "종료" 흐림 처리된다.
  날짜 없이 끝난 행사만 `ended: true`를 쓴다.
- 카테고리는 자유 문자열이고 필터 칩이 자동 생성된다.
  기존: 패션 / 패션잡화 / 스포츠 / 뷰티 / 육아 / 리빙 / 식품 / 반려동물 / 가전.
  새 카테고리를 만들기 전에 기존 것과 통합을 먼저 검토한다 (건강식품→식품으로 합친 전례).
- `source.label` 관례: "딜링크 패밀리세일", "Threads @familysale", "Instagram 제보".

## 커밋 관례

- 메시지는 한국어: "제보 N건 반영: 브랜드1·브랜드2 추가" 스타일.
- `.claude/`는 로컬 전용이라 `.gitignore`로 제외되어 있다.
